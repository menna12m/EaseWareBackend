[build]
builder = "nixpacks"
buildCommand = "npm run build && npx medusa db:migrate"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
